<!--begin::Action--->
<td class="text-end">

    <a class="btn btn-sm btn-light btn-active-light-primary" href="{{ route('roles.permissions') }}#{{ $model->name }}">
        Manage Permission
    </a>
    <!-- <a href ="{{ route('roles.edit', $model->id) }}" class="btn btn-sm btn-light btn-active-light-primary">
        Edit
    </a>

    <button data-destroy="{{ route('roles.destroy', $model->id) }}" class="btn btn-sm btn-light btn-active-light-primary">
        Delete
    </button> -->
</td>
<!--end::Action--->
